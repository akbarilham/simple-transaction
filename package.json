{
  "name": "parkir-vertex",
  "version": "1.0.0",
  "description": "Sistem parkir mobil untuk tugas basis data 2",
  "keywords": [
    "mongodb",
    "typescript",
    "express",
    "atlas"
  ],
  "author": "Akbar Ilham",
  "license": "Apache 2.0",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "npx ts-node-dev --respawn --transpile-only src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier . --write"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongodb": "^4.6.0",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13"
  }
}
